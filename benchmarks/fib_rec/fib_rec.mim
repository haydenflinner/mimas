const TARGET = 30;
const REPEAT = 20;

fn fib(n: int) -> int {
    if n < 2 {
        n
    } else {
        fib(n - 1) + fib(n - 2)
    }
}

let result = 0;
for n in REPEAT {
    result = fib(TARGET);
}
print(result);
